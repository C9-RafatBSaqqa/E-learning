import { useContext } from "react";
import axios from "axios";
import { UserContext } from "../../App";
const Home = () => {
    return (
        <div>
            <h1>Home page</h1>
        </div>
    );
}

export default Home